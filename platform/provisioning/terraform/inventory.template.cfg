[runner]
%{ for host in runner ~}
${host.name} ansible_host=${host.ipv4_address} ansible_connection=ssh ansible_user=ufukty
%{ endfor ~}

[evolver]
%{ for host in evolver ~}
${host.name} ansible_host=${host.ipv4_address} ansible_connection=ssh ansible_user=ufukty
%{ endfor ~}