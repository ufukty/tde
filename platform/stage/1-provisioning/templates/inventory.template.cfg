[runner]
%{ for host in runner ~}
${host.name} ansible_host=${host.ipv4_address} ansible_connection=ssh ansible_user=ufukty ipv4_address_private=${host.ipv4_address_private} app_slug=runner
%{ endfor ~}

[evolver]
%{ for host in evolver ~}
${host.name} ansible_host=${host.ipv4_address} ansible_connection=ssh ansible_user=ufukty ipv4_address_private=${host.ipv4_address_private} app_slug=evolver
%{ endfor ~}

[customs]
%{ for host in customs ~}
${host.name} ansible_host=${host.ipv4_address} ansible_connection=ssh ansible_user=ufukty ipv4_address_private=${host.ipv4_address_private} app_slug=customs
%{ endfor ~}

[api-gateway]
%{ for host in api-gateway ~}
${host.name} ansible_host=${host.ipv4_address} ansible_connection=ssh ansible_user=ufukty ipv4_address_private=${host.ipv4_address_private} app_slug=api-gateway
%{ endfor ~}